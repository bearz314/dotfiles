" Turn on syntax highlighting
syntax on

" Show line numbers
set number

" Blink cursor on error instead of beeping (grr)
set visualbell

" Encoding
set encoding=utf-8

" Change tab to spaces
set expandtab
set tabstop=4

" Change colorscheme
colorscheme desert
